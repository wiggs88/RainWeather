# Mobile interaction implementation plan

1. Expand the Bright Sky radar crop to 200 km and adjust the mobile map center for the shorter panel.
2. Make timeline positions time-based and add pointer capture across the full chart surface.
3. Add a visible selected-time handle and past-to-future drag affordance.
4. Tighten mobile panel, summary, playback, and chart spacing without shrinking touch targets below 44 px.
5. Add timeline pointer interaction tests.
6. Run type checking, tests, production builds, and browser interaction checks.
7. Sync the project repo, push `main`, and verify the Pages redeployment.
